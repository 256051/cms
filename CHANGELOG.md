# 修改记录

## 2026-09-24 — 公众号同步错误诊断

- 同步失败显示具体步骤及脱敏原因，区分获取凭据、封面／正文图片上传、HTTP 状态、DNS、TLS、超时、响应格式与文件读取异常。
- API 日志增加任务、文章、附件编号和失败步骤，不记录 AppSecret、access_token、完整请求地址或原始微信响应；提交异常仍按结果未知处理，避免重复发文。
- 这是诊断信息修正，不代表已确认线上失败原因；无需数据库升级。

## 2026-09-24 — 微信公众号草稿同步与可选自动发布

- 新增管理员「公众号设置」页面，账号配置加密保存在数据库，密钥不回显、留空保留，保存无需重启；Consul／环境变量仅为可选覆盖，覆盖时后台明确提示只读。schema 18 新增独立设置表并纳入备份恢复。
- 发布设置增加「同步到微信公众号（草稿箱）」勾选项，默认不选，未配置时不可勾选；本次选择优先于全局默认，后端同步校验接入状态。
- 文章编辑页增加公众号草稿同步与结果记录；支持手动同步，或配置为随后台、定时及集成 API 发布自动同步。
- 使用已发布快照，上传封面与正文图片，按内容去重；明确失败可重试，提交结果未知时保留核实状态，避免盲目重发。
- 同步默认关闭；schema 17 新增独立同步表，备份恢复保留记录并阻止恢复任务自动重发。
- 新增默认关闭的「同步草稿后自动发布」，只对新同步任务生效；提交后持久化任务编号、查询最终状态，不保存微信文章 URL。已受理任务可跨重启继续查询，未知提交不盲目重发，关闭开关或下架阻止尚未提交的自动发布。
- schema 19 新增独立发布任务表，不修改既有业务表；备份恢复只允许查询已有发布编号，禁止自动重新提交。粉丝群发尚未接入。接入步骤、实际边界与验证方式见 [微信公众号同步](docs/wechat-publishing.md)。

## 2026-09-24 — 数字商品与三渠道收款

- 复用现有产品，增加定价、上架、私有交付文件、单商品订单与凭证查询；旧订单保留原价格和文件。
- 支持支付宝 RSA2 跳转、微信 Native 扫码、Stripe Checkout；后台按渠道配置并加密保存密钥，支持 Consul / 环境变量覆盖。
- 支付签名及金额校验、重复通知幂等、退款状态核对；测试订单不开放正式下载。真实商户实付需配置账号后验证。
- schema 16 新增四张交易表，备份恢复包含订单、文件和密钥。使用范围见 [数字商品与收款](docs/commerce.md)。

## 2026-09-23 — 七项运营完善

- 备份文件列表、历史下载、份数／天数保留和磁盘容量提醒；只在新备份成功后清理，始终保留最新归档。
- 文章接入已有 ZIP 内容包；评论增加所属内容、筛选、管理员回复及原子批量审核；操作记录增加时间、成员、动作和对象筛选及中文描述。
- 新增服务器终端管理员密码恢复，旧会话和 API 令牌失效；通知提供有频率限制的测试发送，沿用持久化结果与重试。
- 新增咨询专员、自己的待办及负责人邮件通知，服务层限制记录归属，补齐后台、集成令牌及草稿附件的权限边界。
- schema 15 增量添加回复与成员邮箱，旧角色和内容保持不变，未新增依赖。使用方法与验证范围见 [运营功能完善](docs/operations-enhancements.md)。

## 2026-09-23 — 友情链接

- 后台「设计与素材 → 友情链接」支持新增、编辑、删除、排序及打开方式，保存后显示在前台页脚。
- 共用全部 15 套主题和页面布局预览，手机端自动换行；没有链接时隐藏该区域。
- 与顶部导航独立，沿用管理员权限、网址校验、版本冲突保护及操作记录；复用现有表结构，无需数据库迁移。见 [使用说明](docs/menus.md#友情链接)。

## 2026-09-17 — 产品、案例与独立页面 ZIP 导入导出

- 三类列表增加勾选导出／筛选结果导出和 ZIP 导入，保留最新草稿、正文、封面与附件、分类标签、SEO、业务字段和页面布局。
- 公共区块展开为已发布的独立模块；导入统一新建草稿并重映射附件、分类和包内链接，地址冲突自动调整，已有内容保持不变。
- 内容包版本、附件签名／SHA256、路径及解压大小均校验；导入整包提交，失败回滚数据和新增文件。沿用管理员／编辑权限、CSRF、审计及历史记录，无数据库迁移。
- 前端转发上限与现有 API／Nginx 上传上限对齐，支持最多 50 MiB 的内容包。使用与验收见 [内容导入导出](docs/usage.md#产品案例与独立页面导入导出)。

## 2026-09-16 — 页面运营与业务工具

- 页面增加撤销／重做、模块富文本、手机独立样式及真实页头页脚预览；公共区块支持独立复制和随发布同步引用。
- 增加邮件／企业微信通知记录、失败重试；页面 SEO、草稿地址修改和旧链接直达跳转。
- 附件支持批量上传、分组、改名及裁剪另存；产品／案例业务字段、可配置咨询字段和导出沿用原有权限与历史机制。
- schema 11–14 自动迁移，兼容 schema 9–14 逻辑备份。配置与验证范围见 [第二轮完善](docs/next-enhancements.md)。

## 2026-09-16 — 页面搭建与模板

- 新增八类页面模块、排序／复制／隐藏、模板库、独立复制应用和桌面／平板／手机预览。
- 页面与模板沿用自动保存、发布快照、定时发布、历史和回收站；模板及仅被模板引用的附件不公开。
- 首页可选择已发布的独立页面，咨询模块复用已有跟进流程；schema 10 自动迁移保留原有页面与主题。见 [页面搭建与模板](docs/page-builder.md)。

## 2026-09-16 — 内容运营完善

- 补齐自动保存、本机恢复、历史版本与回收站；保留历史附件引用，冲突保留输入。
- 新增定时发布/下架、首次发布时间、列表筛选与批量操作、咨询负责人/联系计划/跟进记录/导出。
- 增加正文搜索摘要、上一篇/下一篇、相关文章、RSS、附件检索和引用位置。
- schema 9 增量迁移；新增后台备份、定时执行、失败提醒、校验恢复和访问明细保留策略。审批和图片压缩按本次范围暂不加入。见 [说明](docs/editorial-enhancements.md)。

## 2026-09-16 — 访客 IP 与所属地区

- 后台访客列表显示最近访问 IP 和所属地区，浏览轨迹保留每次访问的网络信息；换 IP 及请求重试不改写旧记录，UV 继续按原浏览器标识去重。
- 服务端复用可信代理解析后的地址，支持 IPv4、IPv6 及映射地址；本机、内网、保留和未知地址明确标注，仅管理员可以读取。
- 新增 IP2Region.Net 3.0.2 和随发布产物附带的离线地址库；不向外部服务发送访客 IP。测试项目的间接依赖 Microsoft.Extensions.DependencyInjection.Abstractions 随该包最低要求从 10.0.0 解析为 10.0.3，未调整其他已有包版本。
- schema 8 为访客、访问明细各增加 IP 和地区字段，旧记录留空；前台采集说明、部署配置和使用说明同步更新。见 [访客 IP 与地区](docs/visitor-location.md)。

## 2026-09-15 — 启动时自动升级数据库

- 新版 API 启动时复用编号迁移，旧数据库升级成功后才提供服务，当前版本直接跳过。失败或数据库版本不兼容时退出。
- 保留首次建站的显式初始化与独立 `--migrate` 命令；升级不创建管理员、不重置文章和访问记录。部署前仍需停止旧实例并备份。

## 2026-09-15 — 访问统计与客户咨询

- 记录实际显示的公开页面浏览，文章阅读次数实时回显；文章后台增加累计 / 今日浏览、独立访客和浏览量排序。
- 后台新增按北京时间查询的 7 / 30 天趋势、自定义日期、来源与设备分布、热门内容，以及访客首次 / 最近访问和浏览轨迹。
- 记录页面可见时长、正文阅读深度、公开附件下载点击与咨询点击；独立于成功提交的咨询数量，排除预览、工作人员和已识别爬虫，支持 DNT。
- 前台新增咨询 / 预约演示表单；管理员可查询客户、查看来源、跟进备注和状态，并删除联系方式。沿用 CSRF、角色授权、版本冲突与审计机制。
- schema 7 只新增统计和客户咨询表；迁移、部署边界、隐私说明与验收入口见 [访问统计与客户咨询](docs/traffic.md)。

## 2026-09-15 — Docker 前端代理修正

- 构建 web 镜像时将 API 和附件代理目标固定为 Compose 的 `api:8080`，修复直接转发到前端容器时访问后端失败的问题。
- 离线包验收增加编译后的代理规则核对及直接访问 Next.js 的 API、附件检查；十五主题 Linux amd64 SQLite 包的 HTTPS 发布与重启验证通过，记录见 `artifacts/package-9a92b65eb0/results.json`。

## 2026-09-15 — 显示模式图标按钮

- 前台显示模式改为 SVG 图标按钮，太阳、月亮、显示器分别表示明亮、黑暗和跟随系统；采用提供的月亮图形，点击循环切换三种模式。
- 保留显示偏好记忆及首屏配色，支持 Enter / 空格操作；按钮提供当前模式、下一模式提示及 44 像素触控区域。
- 手机端图标与导航同行；图标颜色和背景即时切换，避免明暗过渡中对比度不足。

## 2026-09-15 — 全主题明暗模式

- 十五套前台主题新增“明亮模式 / 黑暗模式 / 跟随系统”，默认跟随系统；访客选择保存在自己的浏览器，刷新、新标签页及返回页面时延续选择。
- 服务端读取显示偏好，CSS 直接选择首屏配色；系统变化无需刷新。主题布局、用户设置、后台配色和未提交评论保持不变。
- 统一适配正文默认样式、表格、代码块、菜单、评论、提示与封面占位；按两套背景分别调整主色链接及辅助文字的对比度，不修改内容数据或引入依赖。

## 2026-09-15 — 主题列表与大图预览

- 主题外观改为可搜索的主题列表与大图详情，进入时自动选中当前主题；列表标记当前启用状态，点击预览图可在新标签页查看原图。
- 选中主题后在同一区域调整设置，保留实时预览、保存启用、恢复默认、并发冲突及未保存保护；手机和平板采用上下布局。

## 2026-09-15 — 第二批八套博客主题

- 新增 Chirpy、Oranges、Aircloud、Stellar、Halorum、Aurora、iEmo、Clarity，合计十五套预置主题，分别提供技术侧栏、日期目录、图文卡片、话题列表与三栏阅读布局。
- 复用主题独立设置、管理员预览、原子启用、并发保护、菜单、分类标签、搜索、分页、评论、富文本与正文目录；不增加数据库表、依赖或第三方业务入口。
- 采用八套原作的固定提交，保留完整 MIT / BSD-3-Clause 许可；不带原作文章、图片、字体或 Halo 适配代码，界面不显示原作链接。
- 主题使用与更新方法见 [主题说明](docs/themes.md)，实际验证范围见 [第二批主题验收](docs/theme-batch2-verification.md)。

## 2026-09-15 — 简化主题标识

- 移除后台主题卡片的“原作 · MIT”链接及前台页脚的主题适配标识，保留站点自定义版权和默认 IT猫版权信息。
- 原作来源、完整许可证及版权声明继续随源码和发布包保留；主题样式、配置与数据库结构不变。

## 2026-09-15 — Docker 离线部署包

- 增加 `scripts/package-docker.py`，构建并导出版本化 API、web 和 Nginx 镜像，默认 Linux amd64 + SQLite；发布包携带 Compose、说明、许可、来源版本及 SHA-256 校验。
- 部署时直接导入镜像，无需在服务器编译；管理员凭据和证书由部署方提供，SQLite、附件和认证密钥统一保存在持久卷，不打包本地数据。
- 增加 [离线部署说明](docs/docker-package.md)，同步修正文档中的 schema 版本与主题数量。
- 补充 [宿主机 Nginx 转发方案](docs/host-nginx.md)：仅在回环地址暴露 API/前端端口，由现有 Nginx 终止 HTTPS 并传递真实客户端 IP，无需重新构建镜像。

## 2026-09-15 — 第一批社区主题适配

- 新增 Fuwari、Retypeset、Cactus 三套内置主题：分别提供圆角卡片与侧栏、书籍式阅读与左侧导航、深色等宽日期列表；原有四套主题继续可用。
- 接入已有主题配置、管理员预览、保存启用、版本冲突和操作日志；共享文章、独立页面、分类标签、搜索、分页、评论及丰富正文展示。
- 新主题增加可折叠文章目录与独立的手机布局，正文继续服务端输出；后台增加真实页面缩略图和原作链接。
- 从三套 MIT 原作的固定提交适配，保留完整许可与文件来源；不引入原作运行时、字体、头像或演示文章，不增加依赖。
- 当前 schema 6 无结构变更，不需要额外升级或转换已有内容；更新 API 和 web 后重启即可。
- 来源见 [第三方主题声明](THIRD_PARTY_NOTICES.md)，操作及实际结果见 [主题说明](docs/themes.md) 和 [社区主题验收记录](docs/community-themes-verification.md)。

## 2026-09-14 — 丰富内容编辑器

- 新增接近 Halo 操作方式的分组工具栏与插入菜单，支持 `/` 快捷插入、撤销重做、清除格式、字体字号、颜色高亮、下划线、上下标、对齐与行距；可收起发布设置扩大写作区。
- 新增图片说明/尺寸/对齐、粘贴与拖入上传、附件插入、图片集、视频、音频、受限 HTTPS 网页嵌入、二/三分栏、代码语言、表格行列及合并拆分操作。
- 后端共用 HTML 允许列表净化和媒体类型验证；附件新增 MP4、WebM、MP3、WAV，并提供 HTTP Range；草稿隔离、乐观版本、CSRF 和附件引用保护保持生效。
- 四套主题及预览共用正文样式，手机自动调整分栏；保存失败保留输入，支持 Ctrl/⌘+S 保存草稿和已有未保存离开保护。
- 继续使用 Tiptap 3.31.3 的 MIT 扩展，无付费编辑器或 Halo Vue 插件依赖；不变更数据库结构，schema 仍为 6。现有内容无需转换或重新发布。
- 功能、边界及实际结果见 [编辑器说明](docs/editor.md) 和 [编辑器验收记录](docs/editor-verification.md)。

## 2026-09-14 — Agent 通用发布 API 与访问令牌

- 后台新增“API 访问令牌”：管理员创建、选择关联账号和权限、设置到期时间、查看最近使用并撤销；完整令牌只显示一次。
- 新增 `/api/v1/integration` 内容查询、分类标签、附件上传、创建/更新草稿及发布接口；使用独立 Bearer 认证和分项权限，保留网页 Cookie、验证码及 CSRF 防护。
- 写入通过 `Idempotency-Key` 提供 24 小时去重，业务结果、成功日志和去重记录同事务提交；重试、并发调用及重启后不重复执行成功写入。
- 增加按令牌限流、关联账号启停检查、明确的鉴权/版本/去重错误及令牌归属日志；复用现有 HTML 净化、上传校验、草稿隔离和版本控制。
- schema 5 → 6 显式新增令牌、去重记录及日志归属字段，保留已有内容、账号和站点配置。升级需先备份、停写，更新 API/web 后执行 `--migrate`。
- 更新 OpenAPI、TypeScript 契约、调用文档及 `scripts/publish-article.py` 示例，继续使用 FreeSql，无新增 NuGet/npm 依赖。
- 测试与部署范围见 [本次验收记录](docs/integration-verification.md)。

## 2026-09-14 — 使用 Mapster 替换 AutoMapper

### 修改内容

- 将 AutoMapper 16.2.0 替换为 Mapster 10.0.12（MIT），更新后端及检查项目的 NuGet 锁定文件。
- 保留 `CmsUser → UserView` 账号映射与现有依赖注入方式，公开字段仍为 `id`、`username`、`displayName`、`role`、`enabled`，不返回密码哈希和安全标识。
- 映射配置使用独立实例，要求显式注册映射与目标字段来源；在启动时校验并编译，配置错误会阻止启动。
- 删除启动代码、Compose、环境变量示例和部署文档中的 AutoMapper 许可证配置。
- 显式固定 `Microsoft.IdentityModel.JsonWebTokens` 为原来已解析的 8.14.0，避免移除 AutoMapper 后，SQL Server 驱动共享的 IdentityModel 依赖退回较低版本。
- 增加账号输出回归检查，并在 README 中增加本修改记录入口。

### 部署影响

- 更新后端并重启即可，无需数据库升级；schema 仍为 5，API 契约和前端代码不变。
- 原部署中的 `AutoMapper:LicenseKey` / `AutoMapper__LicenseKey` / `AUTOMAPPER_LICENSE_KEY` 已不再读取，可从本地配置、Consul 或部署环境中删除。
- 本地预览更新前已备份数据库、附件和认证密钥；更新后核对原数据保留，并验证原管理员可经前端代理登录。

### 实际验证

- `dotnet restore --locked-mode`：通过。
- `dotnet build --no-restore`：通过，0 警告、0 错误。
- `dotnet publish src/Cms.Api/Cms.Api.csproj -c Release --no-restore -o artifacts/mapster-publish`：通过；发布目录与运行依赖中已无 AutoMapper，包含 Mapster。
- `dotnet tests/Cms.Checks/bin/Debug/net10.0/Cms.Checks.dll --login-protection`：通过，覆盖验证码生命周期、账号冷却时间及并发限制。
- `python tests/login_security.py`：独立 SQLite 数据库的 7 组 HTTP 检查通过，包含登录、当前用户、账号创建/编辑/列表的字段和值校验，以及 CSRF、验证码和暴力破解防护。结果编号：`login-b3988fb649`。
- `docker compose --env-file .env.example config --quiet`：通过。
- 本地预览数据库全部表记录、附件文件和认证密钥与更新前备份一致；API 就绪检查、登录页面与原账号登录通过。

本次没有重跑四数据库全量验收、浏览器视觉检查或 Docker HTTPS 验收，也没有进行性能基准测试；历史验收结果不代表本次重新执行。
