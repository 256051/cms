services:
  gateway:
    ports:
      - ${CMS_BIND_ADDRESS:-127.0.0.1}:443:443
    volumes:
      - ./deploy/nginx.https.conf:/etc/nginx/conf.d/default.conf:ro
      - ${TLS_DIRECTORY:?Set TLS_DIRECTORY}:/etc/nginx/tls:ro
